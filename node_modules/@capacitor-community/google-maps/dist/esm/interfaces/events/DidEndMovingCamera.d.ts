import { CameraPosition } from "./../../definitions";
export interface DidEndMovingCameraResult {
    cameraPosition: CameraPosition;
}
export declare type DidEndMovingCameraCallback = (result: DidEndMovingCameraResult, err?: any) => void;
